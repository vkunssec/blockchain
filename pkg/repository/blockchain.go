package repository

import (
	"github.com/vkunssec/blockchain/internal/blockchain"
	"github.com/vkunssec/blockchain/pkg/domain"
)

// SaveBlock salva um bloco na blockchain
func SaveBlock(block *domain.Block) (*blockchain.Block, error) {
	return nil, nil
}
